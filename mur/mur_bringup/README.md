# mur_bringup

Questo pacchetto contiene i **launch file** e le **configurazioni** (yaml, rviz, ecc.) per il bringup dei MuR.

## Struttura

- `launch/`: file `.launch` per avvio di simulazione/hardware e stack correlati.
- `config/`: file `.yaml` e altre configurazioni.

