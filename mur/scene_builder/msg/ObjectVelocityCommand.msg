string object_id
# Frame in which velocity is expressed; defaults to planning frame if empty
string reference_frame
geometry_msgs/Twist twist
# Command timeout in seconds; if <=0, use default
float64 timeout
